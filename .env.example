# Database Configuration
POSTGRES_DB=watch_tracker
POSTGRES_USER=watchuser
POSTGRES_PASSWORD=changeme_secure_password
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# Backend Configuration
SECRET_KEY=changeme_long_random_secret_key_at_least_32_characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7
BACKEND_PORT=8000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost

# Frontend Configuration
VITE_API_URL=http://localhost/api
FRONTEND_PORT=3000

# Nginx Configuration
# Note: Use port 8080 if running K3s/Kubernetes on the same machine to avoid port conflicts
HTTP_PORT=8080
HTTPS_PORT=443

# Storage
UPLOAD_DIR=/app/storage/uploads
BACKUP_DIR=/app/storage/backups
MAX_UPLOAD_SIZE=20971520

# Optional: External APIs
# CHRONO24_API_KEY=your_api_key_here
